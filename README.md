### Workspace layers

- `/packages/utils` - Shared packages with stateless no domain-specific business logic
- `/packages/libs` - Shared packages with domain-specific business logic, features. Might be stateful
- `/apis` - Deployable backend services.
- `/apps` - Deployable client applications
